export enum ButtonTheme {
    Primary = 'primary',
    Soft = 'soft',
}

export enum TextButtonTheme {
    // создал временно, предполагаю темную тему мб,
    // но не вижу смысла в софт для этого вида кнопок, можно добавить ночную тему :)
    // щас не добавлял
    Primary = 'primary',
    Night = 'night',
}
